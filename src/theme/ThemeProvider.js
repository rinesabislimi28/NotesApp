import React from 'react';
import { View } from 'react-native';


export const ThemeProvider = ({ children }) => {
    // Placeholder if you later add context-based theming
    return <View style={{ flex: 1 }}>{children}</View>;
};